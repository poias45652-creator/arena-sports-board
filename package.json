{
  "name": "arena-sports-board",
  "version": "3.0.74",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.0.0 <25"
  },
  "scripts": {
    "build": "node arena-prepare.mjs && npm --prefix .arena-app ci --ignore-scripts --legacy-peer-deps --include=dev && npm --prefix .arena-app run build",
    "dev": "node arena-prepare.mjs && npm --prefix .arena-app ci --ignore-scripts --legacy-peer-deps --include=dev && npm --prefix .arena-app run dev",
    "test:source": "node arena-prepare.mjs && npm --prefix .arena-app ci --ignore-scripts --legacy-peer-deps --include=dev && npm --prefix .arena-app run test:source",
    "test:analysis": "node arena-prepare.mjs && npm --prefix .arena-app ci --ignore-scripts --legacy-peer-deps --include=dev && npm --prefix .arena-app run test:analysis",
    "test:render": "node arena-prepare.mjs && npm --prefix .arena-app ci --ignore-scripts --legacy-peer-deps --include=dev && npm --prefix .arena-app run test:render",
    "start": "npm --prefix .arena-app start",
    "restore:source": "node arena-prepare.mjs"
  }
}
